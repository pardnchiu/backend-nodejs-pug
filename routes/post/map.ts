(function () {
  let express = require('express');
  let router = express.Router();

  // router.post('/', require("./post/index"));
  // router.post('*', require(`./404`));

  module.exports = router;
})();
