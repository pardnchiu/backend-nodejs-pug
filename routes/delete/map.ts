(function () {
  let express = require('express');
  let router = express.Router();

  // router.delete('/', require("./post/index"));
  // router.delete('*', require(`./404`));

  module.exports = router;
})();
